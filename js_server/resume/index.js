var resume = {}; //namespace

/**
 * Interfaces
 */
resume.Store = require('./Store.js');

/**
 * Instantiable Classes
 */
resume.JsonStore = require('./JsonStore.js');

/**
 * Expose the namespace object.
 */
module.exports = resume;